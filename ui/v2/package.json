{
    "version": "1.0.0",
    "description": "Evidently UI Monorepo",
    "scripts": {
        "type-check": "pnpm -r type-check",
        "lint": "pnpm -r lint",
        "build": "pnpm -r build"
    },
    "license": "ISC",
    "engines": {
        "node": ">=16",
        "pnpm": ">=8"
    }
}